}
        }
        if(ans.size() == n) {
                }
                if(inDegree[i] == 0) {
                    q.push(i);
            for(auto i : adjList[frontNode]) {
                inDegree[i]--;
            ans.push_back(frontNode);

            q.pop();
            return ans;
        }
        else {
            return {};
        }